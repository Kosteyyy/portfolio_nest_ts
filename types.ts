export type TSkill = {
  name: string;
  img: string;
};

export type TProject = {
  name: string;
  type: string;
  stack: string;
  img?: string;
  link: string;
};

export type TFormData = {
  name: string;
  phone: string;
  // company: string;
  subject: string;
  message: string;
};
